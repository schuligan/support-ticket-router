# Core runtime dependencies.
pydantic>=2.0
rich>=13.0

# Optional — only needed for the real (non-mock) LLM path. The system runs
# fully offline in mock mode without it. Install with:
#   pip install anthropic
# anthropic>=0.40
